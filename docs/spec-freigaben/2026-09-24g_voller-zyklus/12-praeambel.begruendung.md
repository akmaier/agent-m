# The preamble: approval on the dashboard, no empty skeleton

**Found by the main agent on 2026-09-24, reading the whole SPEC.** The preamble still says that
changes are accepted in `scripts/spec_dashboard.py` of the process repository and that "the sections
below are still empty". Both stopped being true on 2026-09-23: Agent M's own SPEC is approved on its
dashboard by a commit (`EIN PRODUKT DARF SEINE FREIGABE IN GIT FUEHREN` in the process repository,
§10 here), and sections 0–10 are filled. A reader who trusts the preamble looks for the wrong tool.

Only the second paragraph changes; the rest of the preamble is byte for byte the current text. The
entry's end anchor is `## 0. Hard product rules`, so nothing below the preamble is touched.
