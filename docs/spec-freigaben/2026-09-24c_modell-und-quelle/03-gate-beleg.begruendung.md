# One sentence in §9 pointed at the withdrawn concept

`THE GATE IS RECORDED` justified itself with "the evidence a regulated profile (§5) has to produce".
"Profile" no longer exists, and the pointer named a section number, which `A REFERENCE NAMES THE
IDENTIFIER, NOT THE POSITION` forbids. The rule itself is unchanged.
