# SPEC approvals — queue 2026-09-24e · participants: people and agents

**PO, 2026-09-24 (UC-002):** *"In step 4, it would be good to have selected different types of
developers / process participants. It can be different human users or agent models. It could range
from an endpoint model all the way to a full agent that lives in a virtual machine or cli
environment. This is important for this process, but we have to be able to configure this as well in
a different use case."*

One entry, appended to §5: participants are configured once per instance (new UC-017), have one of
five types, declare capabilities and where they process data; roles name the capabilities they need.

**Depends on queue `2026-09-24d_quellen-bibliothek`:** `RESTRICTED CONTENT GOES ONLY WHERE ITS SOURCE
PERMITS` uses the licence field that `A SOURCE DECLARES ITS LICENCE` introduces there. Accept `d`
first, or strike this one rule here.

**One rule is my addition, not your instruction:** `RESTRICTED CONTENT GOES ONLY WHERE ITS SOURCE
PERMITS`. See the rationale.

**Impact analysis:** `A PROCESS MODEL ORGANISES PEOPLE AND AGENTS` stays unchanged. The new
`A ROLE NAMES THE CAPABILITIES IT NEEDS` refines it. It is referenced by UC-002, which is updated
together with this queue. UC-003, UC-010 and UC-011 (endpoint, Actions, bridge) remain the technical
connection routes; UC-003 is accepted and unchanged.

**Zieldatei aller Einträge:** `products/agent-m/SPEC.md`

| Nr | Datei | Anker (Überschrift, wortgetreu) | bis (exklusiv) | Commits |
|---|---|---|---|---|
| 01 | `SPEC.md` | ## 5. Process models and practices | — | — |
